# Prod variant: serve the pre-built static SPA from disk (baked in at
# /usr/share/nginx/html by docker/nginx/Dockerfile), falling back to
# index.html for client-side routes.
location / {
  try_files $uri $uri/ /index.html;
}
